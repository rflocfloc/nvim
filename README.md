# nvim
Riccardo's nvim config. Minimal plugins. Uses nvim builtin package manager (v0.12+).

## Requirements
System packages:
  - neovim (0.12+)
  - fzf
  - ripgrep (optional)
  - fd-find (optional)
  - batcat (optional)
