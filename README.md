# nvim
Riccardo's nvim config. No plugins. Check `plugin` branch for version with minimal plugin setup.

## Requirements
System packages:
  - neovim (0.11+)
  - ripgrep (optional)
  - fd-find (optional)
